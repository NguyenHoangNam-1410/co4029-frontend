# AbridgeAI frontend environment.
# Backend target: backend-new/abridgeai (no legacy backend supported).

# API base URL (override for staging/prod). Defaults to local dev.
VITE_API_BASE_URL=http://localhost:8000/api/v1
